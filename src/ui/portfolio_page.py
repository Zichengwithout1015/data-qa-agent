import streamlit as st
import os

st.set_page_config(page_title="AI??? - ???", layout="wide")
st.title("AI?????")
st.caption("??? | github.com/Zichengwithout1015")
st.markdown("---")

# Read and display both portfolio files
base = os.path.join(os.path.dirname(__file__), "..", "..", "portfolio")
files = [
    ("01-Agent????-????????.md", "???????? - ??????"),
    ("02-????-AI???????????????.md", "AI??????????????? - ????"),
]

for fname, _title in files:
    fpath = os.path.join(base, fname)
    if os.path.exists(fpath):
        with open(fpath, "r", encoding="utf-8") as f:
            content = f.read()
        st.markdown(content)
        st.markdown("---")
    else:
        st.warning(f"?????: {fname}")
